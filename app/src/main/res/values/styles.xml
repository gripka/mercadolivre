<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Estilo base do aplicativo -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Outros itens de estilo -->
        <item name="android:dropDownListViewStyle">@style/CustomDropDownListView</item>
    </style>

    <!-- Estilo personalizado para DropDownListView -->
    <style name="CustomDropDownListView" parent="Widget.AppCompat.ListView.DropDown">
        <item name="android:listSelector">@color/menuBackgroundColor</item>
    </style>

</resources>
